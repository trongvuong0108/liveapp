<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu_nav"
    app:startDestination="@id/fragment_home">

    <fragment
        android:id="@+id/fragment_home"
        android:name="com.example.sanpham.Fragment.fragment_home"
        android:label="fragment_home" >
        <action
            android:id="@+id/action_fragment_home_to_fragment_products"
            app:destination="@id/fragment_products" />
    </fragment>
    <fragment
        android:id="@+id/fragment_products"
        android:name="com.example.sanpham.Fragment.fragment_products"
        android:label="fragment_products" >
        <action
            android:id="@+id/action_fragment_products_to_fragment_home"
            app:destination="@id/fragment_home" />
    </fragment>
</navigation>